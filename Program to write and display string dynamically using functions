#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void accept(char *str)
{
    scanf("%s",str);
}
void display(char *str)
{
    printf("%s",str);
}
int main()
{
    char *str;
   str=(char*)malloc(sizeof(char));
    accept(str);
    display(str);
}
